# yaml-language-server: $schema=https://raw.githubusercontent.com/Azure/azure-dev/main/schemas/v1.0/azure.yaml.json

name: chainlit-contract-review-agent
metadata:
    template: azd-init@1.18.2
services:
    src:
        project: src
        host: containerapp
        language: python
        docker:
            path: dockerfile
